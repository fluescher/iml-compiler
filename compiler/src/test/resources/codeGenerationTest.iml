program generationTest
global
	fun inc(i: int32, a: int32)
	returns r: int32
	local 
		var b: int32
	requires [ blubb: a > 0 ]
	ensures [ blah: r > old(i) ]
	{
		b init := a;
		r init := i + b;
		b := -7
	};
	var c: int32;
	var o: int32
{
	! inc(3, 4)
}